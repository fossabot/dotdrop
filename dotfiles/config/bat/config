# Set the theme for syntax highlighting
# --list-languages to show all supported language names and file extensions
--theme="base16-tomorrow-night"

# Configure which elements to display in addition  to  the  file  contents
# Possible values: *auto*, full, plain, changes, header, grid, numbers
--style="plain,grid"

# Specify  when to use colored output.
# The automatic mode only enables colors if an interactive terminal is detected.
# Possible values: *auto*, never, always.
--color="always"

# Specify when to use ANSI sequences for italic text in the output
# Possible values: always, *never*
--italic-text="always"

# Specify when to use the pager
# Possible values: *auto*, never, always
--paging="never"

# Add mouse scrolling support in less (does not work with older
# versions of "less")
--pager="less -FR"

# Specify the text-wrapping mode
# Possible values: *auto*, never, character
--wrap="never"

# Set the tab width to T spaces
# Use a width of 0 to pass tabs through directly
--tabs=2

# Explicitly set the width of the terminal instead of auto determining
# “+/-” prefix to offset the actual width of the terminal
--terminal-width=80

# Map a file extension or file name to an existing syntax
--map-syntax "*.zsh*:bash"
--map-syntax ".ignore:.gitignore"
--map-syntax ".exrc:vim"
--map-syntax "conf:ini"
--map-syntax "imwheelrc:bash"
